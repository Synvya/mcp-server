# Nostr Configuration for MCP Server

# MCP server's Nostr private key (nsec format)
# Generate a key pair at: https://nostrkeygen.com/
# IMPORTANT: Keep this secret secure! It's used to sign events and decrypt messages.
MCP_SERVER_NSEC=nsec1...

# Comma-separated list of Nostr relay URLs
# Default: wss://relay.damus.io,wss://nos.lol
# You can add more relays for better reliability
NOSTR_RELAYS=wss://relay.damus.io,wss://nos.lol

# Timeout for waiting for reservation responses (milliseconds)
# Default: 30000 (30 seconds)
# Range: 1000-120000 (1-120 seconds)
RESERVATION_TIMEOUT_MS=30000
