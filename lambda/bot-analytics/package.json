{
  "name": "synvya-bot-analytics",
  "version": "1.0.0",
  "description": "Lambda function to analyze CloudFront logs for bot traffic to restaurant pages",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && cd dist && zip -r ../lambda.zip . && cd ..",
    "deploy": "aws lambda update-function-code --function-name synvya-bot-analytics --zip-file fileb://lambda.zip",
    "test": "vitest run"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/client-s3": "^3.700.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/aws-lambda": "^8.10.145",
    "typescript": "^5.9.3",
    "vitest": "^4.0.0"
  }
}
